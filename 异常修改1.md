我先按你这套评判标准做一次“代码级体检”：定位参考范围判定、异常方向、科室命中指标、置信度排序这几条链路各在哪个函数，然后给你一份按严重级别排序的问题清单。

搜索正则表达式 `_detect_data_quality_issues|_derive_missing_tests|_weighted_consensus|hit_indicators|reference range|reference_range|status|abnormal|normal|Cr|CysC|K` (`**/langchain_service/**/*.py`)，20 个结果

搜索与 `**/langchain_service/task/*.py` 匹配的文件，6 匹配项

搜索与 `**/langchain_service/knowledge/*.py` 匹配的文件，5 匹配项

搜索与 `**/langchain_service/lab_data_standardization/*.py` 匹配的文件，2 匹配项

搜索正则表达式 `def _detect_data_quality_issues|def _weighted_consensus|def _derive_missing_tests|def _build_llm_main_summary|def _calculate_confidence|def _execute_non_department_actions|def analyze_lab_results|def _normalize_lab_results|hit_indicators|recommended_tests` (`**/langchain_service/task/hierarchical_main_agent.py`)，20 个结果

搜索正则表达式 `def _score_and_rank_departments|def _compute_department_suspicion|def _build_patient_profile|def _parse_json_block|def _retrieve_medical_knowledge|def _analyze_indicators|def analyze\(` (`**/langchain_service/task/lightweight_dept_agent.py`)，8 个结果

搜索正则表达式 `def get_reference_range|def is_value_out_of_range|def classify_abnormality|Cr|CysC|K|creatinine|cystatin` (`**/langchain_service/knowledge/reference_ranges.py`)，20 个结果

搜索正则表达式 `def extract_lab_results|def normalize_indicator_key|status|normal_range|unit|value` (`**/langchain_service/lab_data_standardization/lab_normalization.py`)，20 个结果

读取 [](file:///d%3A/Users/xiaoli/Desktop/MedLabAgent/langchain_service/knowledge/reference_ranges.py#1-1)，行 1 到 260

读取 [](file:///d%3A/Users/xiaoli/Desktop/MedLabAgent/langchain_service/lab_data_standardization/lab_normalization.py#150-150)，行 150 到 305

读取 [](file:///d%3A/Users/xiaoli/Desktop/MedLabAgent/langchain_service/knowledge/reference_ranges.py#260-260)，行 260 到 620

读取 [](file:///d%3A/Users/xiaoli/Desktop/MedLabAgent/langchain_service/knowledge/reference_ranges.py#620-620)，行 620 到 767

读取 [](file:///d%3A/Users/xiaoli/Desktop/MedLabAgent/langchain_service/task/hierarchical_main_agent.py#470-470)，行 470 到 980

读取 [](file:///d%3A/Users/xiaoli/Desktop/MedLabAgent/langchain_service/task/hierarchical_main_agent.py#980-980)，行 980 到 1320

读取 [](file:///d%3A/Users/xiaoli/Desktop/MedLabAgent/langchain_service/task/hierarchical_main_agent.py#1-1)，行 1 到 470

读取 [](file:///d%3A/Users/xiaoli/Desktop/MedLabAgent/langchain_service/task/lightweight_dept_agent.py)

读取 [](file:///d%3A/Users/xiaoli/Desktop/MedLabAgent/langchain_service/task/dept_coordinator.py)

我已经定位到几处关键风险点了，现在再补一轮“精准定位行号”，这样我给你的问题清单会直接对应到代码位置，便于你后续逐条修。

搜索正则表达式 `range_candidates = \[|ref.get\("adult"\),|ref.get\("male"\),|ref.get\("female"\),|severity > 0|if dep == dept and indicator in lab_results and bool\(indicator_info.get\("is_abnormal"\)\)|if cr is not None and is_pediatric and float\(cr\) <= 30|f"- \{ind\}: \{lab_results\[ind\]\} \(\{sev_text\}\)"|"hit_indicators": essential_indicators` (`**/langchain_service/task/*.py`)，20 个结果

搜索正则表达式 `"Cr": \{|"CysC": \{|"K": \{|"adult": \{"min": 60, "max": 115\}|"female": \{"min": 60, "max": 93\}|"normal": \{"min": 0.52, "max": 1.03\}` (`**/langchain_service/knowledge/reference_ranges.py`)，6 个结果

搜索正则表达式 `"cr": \(5, 2000\)|"cystatin_c": \(0.3, 5\)|"k": \(1, 10\)|if not is_plausible_lab_value\(key, value\):\s*continue` (`**/langchain_service/lab_data_standardization/lab_normalization.py`)，3 个结果

搜索正则表达式 `weighted_score =|hit_bonus =|confidence = min\(best_score / denom \+ 0.4, 0.99\)|_add_candidate\(f"复查\{indicator\}"|for ind in \(focus_indicators - hit_indicators\)|if len\(candidate_scores\) < 3` (`**/langchain_service/task/hierarchical_main_agent.py`)，5 个结果

**主要问题（按严重级别）**

1. 严重：异常方向被压扁成“是否异常”，会把低值和高值同等当作同一病理信号  
- 在主链路里，异常包只保存 is_abnormal 和 severity，没有方向字段（高/低）：hierarchical_main_agent.py  
- GAT 选科时只看 is_abnormal，不区分高低方向： hierarchical_main_agent.py  
- 这会直接导致 Cr 低、CysC 低也能强推肾内科“命中”，与你指出的问题一致。

2. 严重：参考范围选择逻辑优先 adult，性别分层基本被覆盖  
- 主Agent 19_64 分层里先取 adult，再 male/female： hierarchical_main_agent.py hierarchical_main_agent.py hierarchical_main_agent.py hierarchical_main_agent.py  
- 科室Agent同样先 normal/adult 再 male/female： lightweight_dept_agent.py lightweight_dept_agent.py lightweight_dept_agent.py lightweight_dept_agent.py  
- 结果是女性 Cr 常被按更宽的 adult 范围判读，误差被系统化放大。

3. 严重：共识置信度有结构性抬高，矛盾证据也可能被包装成高置信  
- 命中指标会额外加 hit_bonus，但命中本身不看异常方向临床意义： hierarchical_main_agent.py  
- 最终置信度公式人为加 0.4 基线，容易把中等证据抬到高置信： hierarchical_main_agent.py  
- 这正是“肾功能不全 82%”这类结果出现的土壤。

4. 高：LLM 输入缺少“异常方向+参考区间+是否关键冲突”的硬约束  
- 科室提示只给“轻/中/重异常”，未明确“高于上限还是低于下限”： lightweight_dept_agent.py  
- 这会让模型把“低 Cr”误读成“肾损伤相关异常”而不自知。

5. 高：数据质量拦截点过窄，几乎没有防住你提到的 OCR/单位错误  
- 目前只针对儿科低 Cr 做特判，成人极低 Cr、单位冲突都没拦： hierarchical_main_agent.py  
- 预清洗仅做“生理可行区间”粗过滤，Cr=28.5 会直接放行： lab_normalization.py

6. 中高：补检推荐逻辑会被“科室焦点-命中差值”牵着走，可能偏离真实主病因  
- 缺失补检里会把 focus_indicators - hit_indicators 当作缺口推进： hierarchical_main_agent.py  
- 候选不足时又塞通用兜底项，可能掩盖证据优先级： hierarchical_main_agent.py

7. 中：参考库配置本身对部分指标边界偏“硬”，缺少“临床低值非致病”语义  
- Cr 和 CysC 当前范围定义较固定，且没有“低值临床意义弱化”规则： reference_ranges.py reference_ranges.py reference_ranges.py

**开放问题 / 假设**
1. 目前 patient_profile 是否稳定提供性别？如果没有，范围选取会继续偏向 adult 默认。  
2. OCR 结果里是否有每项原始参考区间（报告单上的低高值）可用？若有，优先级应高于内置字典。  
3. GLU=13.82 是空腹、随机还是餐后？这会影响“糖尿病主诊断”的置信度标定区间。

**一句话结论**
你给的评判标准和当前代码对照后，问题核心不是某个单点，而是“异常方向缺失 + 参考范围选取偏差 + 置信度抬高”三者叠加，导致正常/偏低指标被误推成高风险诊断。  

如果你同意，下一步我可以按“最小改动不伤功能”的方式给你一版修复清单（先改判定层，不动你现有多Agent架构）。