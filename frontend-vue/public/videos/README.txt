请将要展示的封面视频放在本目录并命名为 intro.mp4。

访问路径（在浏览器中）：/videos/intro.mp4

说明：
- 文件名必须是 intro.mp4，格式建议 MP4（H.264 + AAC）。
- 若需更改路径或文件名，请同时修改 frontend-vue/src/App.vue 中的 video `src` 属性。
- 为了自动播放，建议视频为静音或在第一次加载时包含静音轨道。